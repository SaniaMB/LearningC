#include <stdio.h>
//#include <conio.h>                                         
//void main()
int main(){
    char a , *p;
    unsigned char b , *q;
    int c, *r;
    unsigned int d, *s;
    //clrscr
    
    printf("%lu\t %lu\n",sizeof(a),sizeof(p));
    printf("%lu\t %lu\n",sizeof(b),sizeof(q));
    printf("%lu\t %lu\n",sizeof(c),sizeof(r));
    printf("%lu\t %lu\n",sizeof(d),sizeof(s));

    //getch
    return 0;
}