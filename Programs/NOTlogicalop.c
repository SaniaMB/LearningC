#include <stdio.h>
#include <stdbool.h>

int main(){
   
// logical operator = ! (NOT) reverses the state of a condition
bool sunny = false; // can use false or tue as well

if (!sunny){   // can use false or tue as well

    printf("\n It's sunny outside");
}

else {  

    printf("\n It's cloudy outside");
}


    return 0;
}