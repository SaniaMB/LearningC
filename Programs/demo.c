#include <stdio.h>

int main() {
    int num1 = 5;
    int num2 = 7;
    int sum;

    // Add the numbers
    sum = num1 + num2;

    // Output the result
    printf("The sum of %d and %d is: %d\n", num1, num2, sum);

    return 0;
}
